# git 技能

你知道如何使用 git 进行版本控制。

## 常用操作

- 初始化仓库：`shell` 工具执行 `git init`
- 查看状态：`git status`
- 提交：`git add . && git commit -m "message"`
- 查看日志：`git log --oneline`

## 注意事项

- 提交前先用 `git status` 确认变更
- commit message 使用英文动词开头（Add / Fix / Update / Remove）
